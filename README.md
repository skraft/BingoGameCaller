For playing the game "Bingo". A simple bingo game caller that randomly chooses numbers between 1 and 75 one at a time. The app includes a UI that lets the user pull numbers one at a time and keeps track of the numbers that have already been pulled.

Dependencies:
- PySide6   (python -m pip install PySide6)
